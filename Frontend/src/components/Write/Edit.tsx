import styled from 'styled-components';
import palette from '../../lib/palette';

const EditBlock = styled.div`
  width: 50%;
  height: 100%;
`;

const Edit = () => {
  return <EditBlock></EditBlock>;
};

export default Edit;
